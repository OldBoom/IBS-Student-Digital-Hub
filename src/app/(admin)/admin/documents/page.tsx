// /admin/documents — Admin CRUD for documents.
//
// - Table view: title, semester_min, semester_max, source_type, updated_at.
// - Create/edit form fields: title, description, file_url, semester_min, semester_max, topics (multiselect).
// - file_url must be sanitized before save.

export default function AdminDocumentsPage() {
  return null
}
