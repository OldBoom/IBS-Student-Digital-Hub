// /admin/processes — Admin CRUD for process guides.
//
// - Table view: title, slug, semester range, source_type.
// - Create/edit form: title, slug, overview, steps (ordered list editor), topics, semester range.
// - Steps editor allows adding/removing/reordering Step entries.
// - Each step has: title, description, linked document IDs, optional contact ID, optional warningNote.

export default function AdminProcessesPage() {
  return null
}
