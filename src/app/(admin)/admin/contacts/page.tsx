// /admin/contacts — Admin CRUD for contacts.
//
// - Table view: full_name, department, email, source_type, updated_at.
// - Actions: create, edit, delete (with confirmation modal).
// - Create/edit form fields: full_name, department, email, office_hours, notes, topics (multiselect).

export default function AdminContactsPage() {
  return null
}
