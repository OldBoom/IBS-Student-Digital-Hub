// /admin/help-articles — Admin CRUD for help center articles.
//
// - Table view: title, slug, target_audience, source_type, updated_at.
// - Create/edit form: title, slug, body_md (Markdown textarea), target_audience (All/International/First Semester).

export default function AdminHelpArticlesPage() {
  return null
}
