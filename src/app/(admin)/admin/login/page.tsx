// /admin/login — Admin login page.
//
// - Same form as student login but sets role = 'admin' on success.
// - On success: redirect to /admin/dashboard.
// - Non-admin credentials must be rejected.

export default function AdminLoginPage() {
  return null
}
