// /admin/dashboard — Admin overview page.
//
// - Shows entity counts: contacts, documents, processes, help articles, platforms.
// - Shows recent admin_audit_log entries (actor, action, entity, timestamp).
// - Links to all admin CRUD pages.

export default function AdminDashboardPage() {
  return null
}
