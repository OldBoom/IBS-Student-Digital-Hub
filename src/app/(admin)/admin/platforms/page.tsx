// /admin/platforms — Admin CRUD for academic platforms.
//
// - Table view: name, url, source_type, updated_at.
// - Create/edit form: name, purpose, url, usage_instructions.
// - url must be sanitized before save.

export default function AdminPlatformsPage() {
  return null
}
