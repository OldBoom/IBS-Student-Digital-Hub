// /processes/[slug] — Step-by-step process guide detail.
//
// - Calls GET /api/processes/[slug].
// - Renders ordered list of ProcessStep components.
// - Each step: title, description, linked documents, responsible contact (if any).
// - Warning notes render visually distinct (e.g. yellow banner).

export default function ProcessDetailPage() {
  return null
}
