// /processes — Process guide list.
//
// - Calls GET /api/processes?topicId[]=&semester=.
// - Renders a card per guide: title, overview, topic badges.
// - Links to /processes/[slug] for the full step-by-step view.

export default function ProcessesPage() {
  return null
}
