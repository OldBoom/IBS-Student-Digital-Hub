// /dashboard — Personalized student dashboard.
//
// - Fetch data from GET /api/dashboard (semester-filtered).
// - Display widgets: recent documents, upcoming processes, quick links to platforms.
// - Show logged-in student's semester and student_id in header.
// - Sidebar navigation to all student modules.

export default function DashboardPage() {
  return null
}
