// /contacts — Contact finder by topic.
//
// - Topic selector dropdown.
// - Calls GET /api/contacts?topicId=.
// - Renders ContactCard per result: full name, department, email, office hours.
// - If no contacts mapped to topic → show fallback administrative contact.

export default function ContactsPage() {
  return null
}
