// /search — Full-text search across all content types.
//
// - Search input with 300ms debounce.
// - Calls GET /api/search?q=&type[]=&semester=&topic=.
// - Results grouped by content type (documents, contacts, processes, help articles, platforms).
// - Filter bar: content type checkboxes, semester select, topic multiselect.
// - Empty state and loading state required.

export default function SearchPage() {
  return null
}
