// /documents — Academic document library.
//
// - Calls GET /api/documents?topicId[]=&semester=.
// - Filter bar: topic multiselect, semester select.
// - Renders DocumentCard per result: title, description, semester range, download button.
// - Download button links directly to file_url.

export default function DocumentsPage() {
  return null
}
