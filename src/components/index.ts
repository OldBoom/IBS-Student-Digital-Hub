// Shared UI components — export all reusable components from here.
//
// Components to build (one file each in src/components/):
//
// Student-facing:
//   SearchResultCard    — contentType badge, title, summary, action link
//   ContactCard         — full_name, department, email, office_hours
//   DocumentCard        — title, description, semester range, download button
//   ProcessStep         — step number, title, description, linked docs/contact, warning note
//   PlatformCard        — name, purpose, direct link, usage instructions
//
// Admin shared:
//   AdminTable          — sortable/searchable data table for CRUD list views
//   AdminForm           — generic form wrapper with validation feedback
//   ConfirmDeleteModal  — confirmation dialog before delete actions
//
// General:
//   Button              — primary, secondary, danger variants
//   Input               — text input with label and error state
//   Card                — general purpose content card
//   Badge               — small label (content type, status, audience)
//   PageHeader          — page title + optional action button

// TODO: create each component in its own file, export here
